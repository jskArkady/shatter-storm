{
  "name": "shatter-storm",
  "version": "1.0.0",
  "private": true,
  "description": "A zero-build browser brick breaker with layered Three.js visuals.",
  "scripts": {
    "check": "node --check game-rules.js && node --check three-renderer.js && node --check game.js",
    "test": "node --test tests/game-rules.test.js tests/browser-contracts.test.js",
    "test:browser": "node --test tests/browser-smoke.test.js",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
